/**
 * @author Kate Compton
 */
var Vector, Noise, KColor, TimeSpan;

define(["inheritance", "modules/models/vector",  "noise", "kcolor", "modules/common/timespan"], function(Inheritance, _Vector, _Noise, _KColor, _TimeSpan) {
    Vector = _Vector;
    Noise = _Noise;
    KColor = _KColor;
    TimeSpan = _TimeSpan;
});

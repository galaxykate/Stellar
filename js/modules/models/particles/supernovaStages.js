/**
 * @author April Grow
 * 
 * A collection of functions that create lifespans through the stages of star supernova
 * 1. Star fuses most of its lowest element
 * 2. Star collapses and gains temp until it can fuse its next lowest element
 * 3. Repeat stages 1 and 2 until hitting iron
 * 4. Star fully collapses without any other fusion stages
 * 5. Once dense enough, star bursts forth in full supernova
 * 
 * SNS stands for SuperNovaStage to differentiate the functions in this file from any that may be created in Star.js
 */


define(["inheritance", "modules/models/vector",'lifespan', "modules/models/ui/glow"], function(Inheritance, Vector, Lifespan, Glow) {

	// Step 1
    var SNSFusionOfElement = function(star) {
    	
    };
	
	// Step 2
    var SNSCollapse = function(star) {
    	
    };
    
    // Step 4
    var SNSFullCollapse = function(star) {
    	
    };
    
    // Step 5
    var SNSExplode = function(star) {
    	
    };

    //return {
    //    
    //};

});
